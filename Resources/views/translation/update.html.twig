{% extends 'base.html.twig' %}

    {% block body %}
      <h1>Modifier la traduction pour {{ entityName }}</h1>
      {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit">Mettre à jour</button>
      {{ form_end(form) }}
    {% endblock %}
